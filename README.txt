Steps:
1.) Open Terminal
2.) Clone repo
3.) Run “./runMe” in Terminal
4.) The webapp should automatically open. If not, point browser to localhost:8080. Point browser to localhost:8180 for backend


NOTE: The backend is using mLab as a frontend for the MongoDB database. In development, it does not have redundancy detection. It will add data even if it already exists.



